# Other unmentioned requirements

### LevelDB

Python uses LevelDB to access plyvel, the database wrapper. If you don't have it, it is crucial that you install it via brew in order to run the chain.

```
brew install leveldb
```
